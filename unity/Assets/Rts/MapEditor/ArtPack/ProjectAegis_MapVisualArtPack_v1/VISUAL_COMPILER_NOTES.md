# Visual Compiler Notes

This pack preserves every mesh filename currently referenced by `AegisMapArtPack` and adds semantic material metadata for the visual compiler. Use it with theme-driven terrain chunks, topology-driven cliffs, field-based resources, modular pads, and rule-based scatter.
